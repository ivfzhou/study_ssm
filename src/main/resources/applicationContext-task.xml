<?xml version="1.0" encoding="UTF-8"?>
<!--
    Quartz 定时任务配置文件（父容器）。
    配置内容：定时扫描 task 包下的组件、定义生日祝福邮件发送任务。
    Cron 表达式 "0 53 * * * ?" 表示每小时第53分钟执行一次。
-->
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="cn.ivfzhou.ssm.task"/>

    <!-- 2. 任务描述 -->
    <bean id="jobDetail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="happyBirthdayTask"/>
        <property name="targetMethod" value="sendBirthdayEmail"/>
    </bean>

    <!-- 3. 任务执行周期 -->
    <bean id="trigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="jobDetail"/>
        <property name="cronExpression" value="0 53 * * * ?"/>
    </bean>
    <!-- 4. 安排到执行表中 -->
    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <array>
                <ref bean="trigger"/>
            </array>
        </property>
    </bean>

</beans>
